# config/teams/meeting-policies.yaml
# Desired state for Teams Meeting Policies.
#
# STATUS: Schema under validation — Teams UTCM properties are being confirmed against
# the actual API schema. Only fully validated properties should be uncommented.
#
# Key findings from API validation:
#   - Teams policies require 'Identity' not 'Id' as the key property
#   - Not all Graph policy properties are in the UTCM monitor schema
#   - Run a snapshot to confirm supported properties:
#     pwsh -Command "./scripts/Take-Snapshot.ps1 -Resources @('microsoft.teams.meetingpolicy')"
#
# Reference: https://learn.microsoft.com/en-us/graph/utcm-teams-resources#meetingpolicy-resource-type

resources:
  - resourceType: microsoft.teams.meetingpolicy
    properties:
      Identity: Global
      DisplayName: "Global Meeting Policy"
      AllowAnonymousUsersToJoinMeeting: false
      AllowAnonymousUsersToStartMeeting: false
      AllowExternalParticipantGiveRequestControl: false
      AllowMeetingRegistration: true
      AllowNetworkConfigurationSettingsLookup: false
      AllowOrganizersToOverrideLobbySettings: false
      AllowOutlookAddIn: true
      AllowParticipantGiveRequestControl: true
      AllowPrivateMeetingScheduling: true
      AllowRecordingStorageOutsideRegion: false
      AllowTranscription: true
      AutoAdmittedUsers: EveryoneInCompanyExcludingGuests
      VideoFiltersMode: All
